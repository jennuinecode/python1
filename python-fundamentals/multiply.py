# a = [2, 4, 10, 16]
# def multiply(array):
#     for num in range(len(array)):
#         array[num] = array[num] * 5
#     return array
#
# b = multiply(a)
# print b

def stringInString(a,b)
    for i in range (a):


stringInString('snap','ohsnap')
